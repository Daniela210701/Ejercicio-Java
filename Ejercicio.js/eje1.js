let arr = ["This", "is", "a", "Sentence."];

function printOutString() {
  // Utiliza el método join() para concatenar los elementos del array en una cadena
  let oracion = arr.join(" ");
  alert(oracion);
}

printOutString();
